<% provide(:title, 'All groups') %>
<h1>All groups</h1>

<ul class="groups">
  <% @groups.each do |group| %>
    <li>
      <%= link_to image_tag( group.image, height: 50), group %>
      <%= link_to group.name, group %>
      <%= link_to "delete", group, method: :delete, data: { confirm: "You sure?" } %>
    </li>
  <% end %>
</ul>